services:
  - type: web
    name: boblox-hub
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 10000
    disk:
      name: data
      mountPath: /opt/render/project/data
      sizeGB: 1
